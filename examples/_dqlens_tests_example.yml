# Auto-generated by dbt-dqlens. Review and commit to your repo.
# Generated at: 2026-05-05T10:00:00Z
# Profile baseline: .dqlens/baselines/profile_20260505_100000.yaml

version: 2

models:
  - name: orders
    tags: [dqlens]
    columns:
      - name: id
        tests:
          - unique
          - not_null

      - name: email
        tests:
          - dqlens_no_null_drift:
              baseline_pct: 0.1
              threshold_multiplier: 3
          - dqlens_no_empty_strings:
              max_pct: 30

      - name: amount
        tests:
          - dqlens_no_outliers:
              lower_bound: -110.0
              upper_bound: 210.0

      - name: customer_id
        tests:
          - dqlens_no_orphans:
              target_model: ref('customers')
              target_column: id

  - name: customers
    tags: [dqlens]
    columns:
      - name: id
        tests:
          - unique
          - not_null

      - name: phone
        tests:
          - dqlens_no_empty_strings:
              max_pct: 30

      - name: signup_source
        tests:
          - accepted_values:
              values: ['web', 'mobile', 'referral', 'organic']
              # DQLens detected 4 distinct values (low cardinality enum)
